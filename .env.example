# 複製這個檔案成 .env 後填入實際值。.env 已列入 .gitignore，不會進版控。
#
#   cp .env.example .env        # Git Bash
#   copy .env.example .env      # PowerShell / cmd

# SQL Server SA 密碼
# 規則：至少 8 個字元，且必須包含大寫、小寫、數字、符號其中三種以上，
#       不符合的話容器會啟動失敗（docker compose logs mssql 看得到錯誤）
MSSQL_SA_PASSWORD=

# Riot API 開發用金鑰（開發者平台申請，24 小時失效）
#
# 這個值「只在本機用 docker compose 跑」時會被讀到——
# compose 讀走它，設成 API 容器的環境變數 RiotApi__ApiKey。
#
# 其他兩種情況都不看這個檔案：
#   本機直接跑 API（dotnet run）→ User Secrets
#   正式環境                    → 由部署平台注入環境變數，不會有 .env 這個檔案
RIOT_API_KEY=

# ASP.NET Core 環境設定
# docker compose.yaml 內的環境變數 ASPNETCORE_ENVIRONMENT=${}。
# 測試環境 : Development 會讀 appsettings.Development.json (個人開發用)
# 正式環境 : Production 會讀 appsettings.Production.json (正式部署用)
ASPNETCORE_ENVIRONMENT=Development
#ASPNETCORE_ENVIRONMENT=Production
